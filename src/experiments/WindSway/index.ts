import { Experiment } from '..';
import { Scene } from './Scene';

export default {
  title: 'Wind sway',
  id: 'windSway',
  description: 'Wind effect visualization',
  Component: Scene,
} as Experiment;
